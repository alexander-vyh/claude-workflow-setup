## 1. Independent Native Corpus

- [ ] 1.1 Build the redacted, pre-normalizer intervention corpus and freeze its
  expected-label manifest before normalizer work. Record installed binary versions,
  native event references, and fixture hashes outside adapters. Define an independent
  metamorphic contract for fields known to be irrelevant, but do not generate its runtime
  variant yet. Include two different command shapes with the same semantic authority and
  a same-prefix pair whose scope-expanding member still requires intervention. Cover the
  supplied recent Codex incidents plus controlled
  Claude Code and Pi fixtures, including approval, edit confirmation, informational
  question, genuine
  consequential block, unknown-payload, dynamic-argument, replay, and missing-coverage
  controls; label semantic action, intervention kind, wait scope, expected authority
  source, and native provenance without calling production classifiers. (specs:
  `Normalize fixture-proven native interventions`, `Record expected semantic authority
  independently`, `Adapter readiness requires installed-version fixtures`)

## 2. Observer and Time-Window Query

- [ ] 2.1 Implement the strict observer-only event model, manifest-owned fixture adapters,
  atomic per-session journal, replay deduplication, and public cross-session time-window
  query. Prove one-to-one corpus reconciliation, semantic grouping across dynamic
  arguments, derived pending attention, honest incomplete-coverage reporting, secret
  redaction, and zero normal-session permission/continuation side effects. Add
  architecture checks that adapters cannot supply authority verdicts, future authority
  boundaries cannot accept raw command text or prompt prose, and readiness requires
  independent version/native-event/fixture evidence. Only after implementation, generate
  and evaluate the runtime metamorphic variant from the frozen base event and contract so
  fixture-command hardcoding fails. (specs: `Keep normalization
  non-authoritative`, `Query all observed interventions in a time window`, `Reconcile
  query results to native truth`, `Derive pending attention from canonical events`,
  `Avoid duplicate human attention`, `Adapters do not own policy`)

## 3. Controlled Two-Actor Outcome Probe

- [ ] 3.1 Run the fixture-proven supported-host scenario with a synchronization barrier:
  first prove from native requested/resolved evidence that the informational intervention
  is visible and unresolved; only then make an explicitly disjoint reversible sibling
  ready and have the same Escapement supervisor dispatch it. Independently verify its
  ready-to-running transition, first tool call, and effect occur before resolution while
  one unresolved consequential action never executes. After the sibling effect, re-check
  independently that the native card remains visible, no decision was submitted, and the
  affected action remains waiting. Run a second control with no
  runnable sibling and verify `input_required`. Reconcile native events exactly once
  through the public query and publish the redacted outcome evidence and capability gaps.
  (specs: `Represent pending attention without global suspension`,
  `Preserve consequential blocking control`, `Limit continuation enforcement to the
  controlled slice`, `Derive pending attention from canonical events`, `Query all
  observed interventions in a time window`)
