## 1. Oracle and behavioral contract

- [x] 1.1 Record the Test Oracle Brief with independent Git-state controls and mutation challenges
- [x] 1.2 Add failing public-command tests for exact clean fast-forward and current-root behavior
- [x] 1.3 Add failing negative controls for dirty, divergent, non-default, detached, and non-primary repositories
- [x] 1.4 Add failing lifecycle controls proving fresh worktree creation and completed finish remain independent of root refusal

## 2. Primary-checkout lifecycle

- [x] 2.1 Implement structured root diagnosis and verified fast-forward against a resolved remote-default SHA
- [x] 2.2 Add the public `sync-root --repo` JSON command and fail-closed exit behavior
- [x] 2.3 Integrate non-blocking root synchronization with default-source create and completed finish transactions

## 3. Agent surfaces

- [x] 3.1 Stage the root lifecycle runtime in generated Claude and Codex plugin packages
- [x] 3.2 Add package-parity and root-protection registration controls, then regenerate committed surfaces

## 4. Verification and landing

- [x] 4.1 Run focused behavioral, mutation, generated-surface, and full repository verification
- [x] 4.2 Exercise the public command against a disposable real remote and primary checkout
- [x] 4.3 Push the feature branch, merge the green pull request, refresh both installed plugins, and verify the installed outcome
