## 1. Independent Contract Tests

- [x] 1.1 Add a focused mission-capability test module whose expected mission, capability chain, ordinary means, and consequential boundaries come from the approved OpenSpec contract rather than production helpers.
- [x] 1.2 Add copied-repository negative controls using fixed test-owned mission, capability, doctrine, and generated-target inventories; corrupt each surface independently and prove each failure names the omitted or divergent path.
- [x] 1.3 Add positive controls proving explicit adapter sections may still name OpenSpec, Beads, worktrees, current clients, and GitHub while the core remains tool-neutral.
- [x] 1.4 Add executable controls for the current green-merge, `confirm_class`, deploy-metadata, and Codex final-response support statuses, and parameterized digit/word/hyphenated mutable-count mutants.

## 2. Canonical Identity And Generated Surfaces

- [x] 2.1 Add a fail-closed `agent-surfaces/identity.json` with the approved mission, operating loop, durable capabilities, principles, and current adapter mapping.
- [x] 2.2 Make the renderer consume and validate the identity source for generated instruction and plugin metadata; a valid sentinel mission in a copied repository must propagate to all generated identity targets.
- [x] 2.3 Add the shared delegated-authority and action-local-continuation onboarding doctrine, register it in the authored manifest, and clarify that declared task branches and ordinary landing actions are already authorized.
- [x] 2.4 Regenerate `AGENTS.md`, `CLAUDE.md`, plugin trees, marketplaces, and compatibility surfaces solely from their authored owners.

## 3. Runtime Doctrine Corrections

- [x] 3.1 Bound outcome ownership and agent-team continuation to causally necessary blockers; require adjacent discoveries to be tracked without expanding execution scope.
- [x] 3.2 Correct continuation and landing prose so stored `confirm_class`, green status, deploy metadata, and final-response support are described only at their actually enforced level.
- [x] 3.3 Make molecule gates, wind-down guidance, and blocking language reserve human attention for unresolved consequential choices and continue independent authorized work.

## 4. Public Documentation Alignment

- [x] 4.1 Rewrite the README identity and operating model around the mission and durable capabilities, retaining a clear current-adapter mapping and truthful installation/support sections.
- [x] 4.2 Make vocabulary definitions client-neutral and align naming rationale with capacity, delegated authority, controlled progress, and verified delivery.
- [x] 4.3 Update the deck identity and architecture narrative, remove or derive mutable workflow/skill/hook counts, and remove stale TeamCreate and legacy installation claims.

## 5. Verification And Delivery

- [x] 5.1 Validate the OpenSpec change strictly and run the mission-capability, merge-authorization, repository-outcome, Codex final-response-gap, and agent-surface test suites, including every planted negative and positive control.
- [x] 5.2 Run the renderer drift check and the repository's broader relevant test/lint suite; inspect generated and authored identity surfaces for semantic agreement and truthful adapter gaps.
- [x] 5.3 Update and close the tracked Bead only after the exact documentation, generated-package, and installed-adapter outcome is verified.
- [x] 5.4 Commit and push the task branch, open the pull request, repair all causal review or CI failures, and merge under the declared repository authorization.
- [x] 5.5 Run the repository's declared deployment/refresh path and verify the effective installed Codex and Claude adapters expose the new mission and doctrine without claiming native prompt suppression or scheduler enforcement.
